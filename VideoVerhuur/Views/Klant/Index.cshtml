@model VideoVerhuur.Models.Klanten

@{
    ViewData["Title"] = "Welkom";
}@{
    ViewData["Title"] = "Welkom";
    var genres = ViewBag.Genres as IEnumerable<VideoVerhuur.Models.Genres>;
}
@await Component.InvokeAsync("WelkomsBericht")

<h3>Kies een genre</h3>
@if (genres != null && genres.Any())
{
    <ul class="list-unstyled" style="display: flex; flex-wrap: wrap; gap: 10px;">
        @foreach (var genre in genres)
        {
            <li style="list-style-type: none;">
                <form asp-controller="Klant" asp-action="Index" method="post" class="d-inline">
                    <button type="submit" name="genreId" value="@genre.GenreId" class="btn btn-outline-primary">
                        @genre.GenreNaam
                    </button>
                </form>
            </li>
        }
    </ul>
}
